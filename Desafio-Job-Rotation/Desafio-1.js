var [k, soma, indice] = [0, 0, 13]  // Declaração das variáveis

while (k < indice){  // Loop somando os valores de K
  k+=1;
  soma+=k;             
}

console.log(soma)        //Exibe valor da soma no terminal

//A resposta é 91